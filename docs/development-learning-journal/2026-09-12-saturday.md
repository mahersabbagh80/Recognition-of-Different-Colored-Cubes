# Saturday, 12 September 2026 — Guided training and live evaluation

## Workflow preparation completed

Maher requested a saved Markdown guide before making changes or running training. Created [Training and validation workflow](../training-and-validation-workflow.md), separating training, checks on reused photos, and evaluation on fresh live robot scenes.

The agreed next experiment will use all 26 approved images, including the four formerly held out. Maher will perform the edits and execute commands with step-by-step guidance. No additional capture batch is a prerequisite for this training run. Live deployment remains part of the objective.

Read the existing training script to confirm that it still references the historical dataset and split, uses validation-based early stopping, and selects the best checkpoint. Those assumptions need explicit review before the new run. No training code, dataset configuration, or model was changed during this documentation step. No training or evaluation command was executed.

The new guide records the intended inputs and outputs, the training loop, the distinction between training checks and independent evaluation, and the need to verify live location outputs separately. It does not claim the future steps are complete.

**Next learning checkpoint:** inspect the current dataset configuration together, then prepare a separate configuration for all 26 approved images.

## Revised selection: first frame per setup

Maher chose to keep the approved frame ending in `_0001.jpg` from each of the 13 setups and exclude the later selected frame from the next run. This supersedes the all-26 proposal above. Recorded the per-file keep/exclude mapping in [the selection record](../../evaluation/robot_dataset_2026-09-09/first-frame-selection-2026-09-12.md). Checking the original manifest confirmed 13 selected images, 27 annotated cubes, and one empty background. No photos or labels were deleted and no training configuration was changed.

The blue/green pair was visually inspected and appears virtually unchanged. The selection rule for other groups is user-approved; it is not a verified claim of identical pixels across every pair. Next: guide Maher in configuring training to use only this selection, rather than both complete folders.

### Immediate scope correction

Maher clarified that the first-frame instruction concerns only the existing training folder. The preceding all-13 interpretation was too broad. Confirmed scope is 11 kept and 11 excluded training-folder images; the four validation-folder images remain pending a separate decision. Corrected the selection record. No dataset files or configuration were changed.

## User removed later frames from exported dataset

Maher extended the selection to both validation pairs and moved 13 later-frame JPGs to Trash using the editor. Read-only filesystem verification found 11 training-folder images and two validation-folder images; all end in `_0001.jpg`, and every remaining image has its label. Thirteen unmatched label files still remain (11 train, two val); their cleanup is the next guided step. The historical split manifest now references removed exported images, so the old training script integrity checks need adaptation before execution. No training was launched. The YAML still references both folders, which now contain 13 images total despite its all26 filename/comments.

### Label cleanup verified

Maher moved the 13 corresponding later-frame label files to Trash. Verified exact image/label stem equality: 11 pairs in train and two in val, all first frames, with no unmatched JPG/TXT files. The saved YAML includes both folders for both training and reused-image checks, so it now selects 13 images. Its all26 name/comments are stale. No training was executed; historical manifest checks still need adaptation.

### Selected-13 configuration saved

Maher renamed the configuration to `evaluation/results/robot_training_2026-09-09/data_selected13.yaml` and updated both comments to 13. Read back the saved file: train and val each reference images/train and images/val; color IDs remain blue=0, green=1, red=2. These folders contain the previously verified 13 matching pairs. Thus both phases use the same images and reported metrics are training-set checks. The training script has not yet been connected to this configuration.

### Training script dataset reference updated by Maher

Verified the saved edit in `scripts/run_robot_training_experiment.py`: DATA is no longer imported from the smoke script; it is defined as ROOT / "evaluation/results/robot_training_2026-09-09/data_selected13.yaml". The existing data=str(DATA) training argument will use this configuration. No training was run. Next: restrict the preflight manifest iteration to the explicitly chosen first frames while retaining integrity checks.

### All selected examples consolidated into training folders

At Maher's request, he moved the first-frame dist40cm and spread_out JPGs from images/val into images/train and their matching TXT labels from labels/val into labels/train. Verified 13 matching image/label stems in training and both validation directories empty. YAML still lists both directories and needs updating. Historical manifest paths for the two moved examples are stale; the proposed script check must accommodate relocation while preserving approval verification. No training executed.

### Consolidated YAML verified

Read back Maher's saved YAML: train and val now both point to images/train, with the comments explicitly describing a training-set check. Color mapping and dataset root remain unchanged. The script still has the original 26-record integrity loop; no replacement loop has yet been saved and no training run has started.

### Folder-driven preflight applied with authorization

Maher authorized Codex to apply the explained replacement in `scripts/run_robot_training_experiment.py`. Added pathlib.Path and yaml imports. The check reads DATA with yaml.safe_load, enumerates JPGs directly in the configured training directory, and looks up each basename in the historical approved manifest. It verifies unique approved filenames, nonempty existing image directory, known filenames, image hashes, and the existing label row-count/finite-number checks using current labels/train paths. The historical JSON is unchanged. This implementation supports the current single-directory JPG layout; it does not validate label coordinates against approved boxes beyond the existing structural checks.

Verification: used `.venv-m2/bin/python` with an inline AST-based harness to parse the saved script and execute only its dataset-check block (from loading split through the image loop). All 13 images passed. No model loading, GPU operation, or training was performed. Stopping/checkpoint-selection settings and the old summary wording still require review before training.

## Methodology review requested before training

Paused implementation at Maher's request to review training/validation methodology. Read the local training-and-evaluation reference, lesson 0006 on generalization/data splits, and RESOURCES.md; checked official PyTorch transfer-learning and scikit-learn cross-validation documentation. Inspected installed Ultralytics trainer, EarlyStopping, and detection-metric code. Current detection fitness is mAP50-95; patience=15 monitors it, and the trainer saves best according to that fitness. The current YAML evaluates the training images, so those scores measure training-set fit. They neither establish generalization nor prove memorization. val=False alone would not remove all validation: the trainer also validates at the final epoch.

Recommendation for discussion: separate representative scene groups for development validation, then use fresh live robot scenes to assess the selected system. Folder separation is optional; sample separation matters. Training on all selected images can be a later refit after settings/stopping policy are fixed, with new external evaluation, or a clearly labeled prototype compromise. No split, training settings, or model was changed during this research.

Research saved in [Training and validation methodology review](../training-validation-methodology-review.md). The lead completed the note after interrupting the helper to keep the review bounded; the recommendation is based on the lead's directly inspected project references, official sources, and installed code. Next action awaits discussion of sample separation; no new run was started.

## Separate capture checklist prepared

Maher chose to clean the usable room area and collect new training and validation photos. Created [indoor capture checklist](../indoor-cube-capture-checklist.md) with camera-relative placement, approximate distances, background/lighting instructions, 18 training rows and nine separately reconstructed validation rows, and a first setup to review together. Keep the existing 13 approved images in training; planned total after annotation is 31 training and nine validation. Updated the Sunday plan with this proposed revision. No images have been captured or imported, and YAML/code were not changed for the new split. Counts are a manageable proposal, not evidence of adequacy.

## Individual visual review completed

At Maher's request, opened all 13 retained JPGs individually with view_image at native 640x360 resolution. Recorded image-by-image observations and checklist comparisons in [the visual review](../existing-13-image-visual-review.md). Existing photos already cover central singles, all pairs, three-cube rows at varying apparent sizes, a blue-front triangle, spread-out cubes, and an empty floor. Exact distances were not inferred from pixels. Main observed gaps are off-center singles, blue-left arrangements, and changed background framing. Revised the capture proposal from 18 to 10 new training photos, retaining nine fresh validation photos; expected future total 23 train/9 val after annotation. Updated plan and checklist. No photos or labels were changed and no training ran.

## Live preview established before T01 capture

Maher arranged the blue cube and requested a live preview before saving a training image. The initial desktop rqt instructions incorrectly used `setup.bash` in his zsh terminal. Reading the app terminal confirmed that this attempted to source a nonexistent `setup.sh` in the working directory. Inspecting the installed scripts explained the failure: the bash variant uses `BASH_SOURCE`, whereas the zsh variant uses zsh's script-path syntax. Running `source /opt/ros/humble/setup.zsh` in zsh succeeded, and `ros2 pkg prefix rqt_image_view` returned `/opt/ros/humble`. The working directory itself is not a requirement for the viewer.

The agent's first `ros2 topic list -t` failed because its sandbox prohibited sockets. This does not establish that Maher's interactive app terminal has the same restriction; the earlier suggestion that the app terminal caused the network problem was unverified. An authorized read-only query outside the sandbox returned controller and lidar topics, but no RGB topic.

Read-only SSH checks used `ssh -o BatchMode=yes -o ConnectTimeout=6 -o StrictHostKeyChecking=yes jetrover bash --noprofile --norc` with `source /opt/ros/humble/setup.bash`, `systemctl is-active start_app_node.service`, `ros2 topic list -t`, and `ros2 node list` on the robot. The service was active, the robot listed `/depth_cam/rgb/image_raw`, and its graph included `/web_video_server`. A bounded `timeout 5 ros2 topic hz /depth_cam/rgb/image_raw` observed approximately 19–21 Hz; timeout exit 124 ended the measurement intentionally. Thus the RGB publisher was delivering frames on the robot, despite the earlier incomplete desktop graph. The reason for that graph discrepancy and the exact rqt UI symptom remain unresolved; they were not proven to be caused by the shell setup error.

Queries for the web server's `port` and `address` parameters returned `Parameter not set`. A read-only `curl --max-time 8 --silent --show-error http://192.168.2.138:8080/` succeeded and listed an RGB stream-viewer link. Opened `http://192.168.2.138:8080/stream_viewer?topic=/depth_cam/rgb/image_raw` in the visible in-app browser, confirmed a rendered camera image by screenshot, and kept the tab open. This reuses the already-running LAN web server; no robot services, firewall settings, or software installations were changed.

Updated the capture checklist with this verified preview route and shell-specific setup instructions. No new training photo was saved or annotated, and no training ran. Next learning checkpoint: Maher confirms the preview updates when he moves the blue cube, then checks T01 framing before capture. Preview availability is not a detector-performance result.

## T01 training photo captured and reviewed

Maher used the browser preview to arrange one blue cube approximately 40 cm from the camera in the left part of the image under the normal yellow room lighting. The documented Jetson project path did not exist on the current robot. Maher therefore copied the repository's `scripts/capture_frames.py` from the desktop to `/tmp/capture_frames.py` on the JetRover with `scp`, then connected with `ssh jetrover` and loaded the ROS base and vendor overlays for zsh.

From the JetRover terminal, Maher executed the one-frame capture command using `/depth_cam/rgb/image_raw`, output root `/home/ubuntu/cube_camera_samples/new_training`, a 15-second timeout, and prefix `T01_take1`. The script created the dated capture directory, saved `T01_take1_0001.jpg`, wrote `T01_take1_metadata.json`, and exited after one frame. The sidecar records a 640x360 `rgb8` frame, 66,442-byte JPEG, optical frame `depth_cam_color_optical_frame`, and SHA-256 `3cfe68d5a71f421c632a89554f1927bc9298eceb5286405bc327bc1ab78c1b87`.

Codex copied the JPG to `/tmp` for visual inspection. The image showed the complete blue cube in the left image region, separated from the background and with no hand visible. Maher reviewed the displayed JPG, accepted its color, sharpness, and complete edges, and confirmed that the yellow illumination is the normal lighting condition for this work. T01 is therefore retained as a candidate training image. It has not yet been transferred into the repository dataset, annotated, or used in training. The capture verifies data collection, not detector quality.

## T02 training photo captured and reviewed

Keeping robot position A and the same normal yellow room lighting, Maher moved the blue cube to the right part of the preview at approximately 75 cm. He ran the same one-frame capture workflow with prefix `T02_take1`. The script saved `T02_take1_0001.jpg` and its metadata sidecar under `/home/ubuntu/cube_camera_samples/new_training/2026-09-12/T02_take1/`. The sidecar records a 640x360 `rgb8` frame, 67,480-byte JPEG, and SHA-256 `d606a5a3b273b5e39692a893ba830f9e16c1c804831ac0d362d7b8176a6daea9`.

Codex copied and opened the actual JPG. Compared with T01, the cube was visibly smaller, fully contained, and located in the right image region; its color and edges remained visible. Maher approved the image. T02 is retained as a candidate training image, but has not yet been imported, annotated, or used for training. At Maher's request, the remaining training captures may now proceed independently using the checklist, with review when he reports completion or a problem.

## Remaining capture batch transferred and initially reviewed

Maher reported completing the remaining planned photos except V09 and asked Codex to inspect and copy them from the JetRover. A read-only remote listing found 17 JPGs and 17 metadata sidecars under `/home/ubuntu/cube_camera_samples/new_training/2026-09-12/`: T01-T09 and V01-V08. No separate `new_validation` directory existed, and no T10 or V09 file was present. Codex copied the raw directory to `evaluation/camera_samples/cube_training_validation_2026-09-12/raw/new_training/`; this repository path is ignored by Git. All 17 copied JPG SHA-256 values matched their metadata sidecars.

Codex produced separate training and validation contact sheets and visually inspected the transferred images. T01-T08 show the requested cube colors, placements, and complete cube outlines. V01-V06 each show three complete cubes in varied layouts and depths; V07 is cube-free; V08 contains a large ordinary non-cube object. No obvious blur or clipping made these images unusable in the overview. This is an initial visual check, not Maher's final annotation approval.

The two planned negative training rows were captured in the opposite semantic order from the initial table: T09 is the empty-floor image, and T10 contains an ordinary non-cube object. The checklist now records this actual assignment. V09 requested a second normal lighting condition, but Maher has only one normal room-light condition; it is therefore omitted rather than manufactured with unrealistic lighting. The resulting eight-image validation batch still contains six positive and two negative scenes. The V-prefixed files were physically saved below `new_training`, but their prefixes preserve the assigned validation split; curated import must keep them out of training.

Maher then captured T10 with a black spray container as the ordinary non-cube foreground object and no cubes. Codex copied `T10_0001.jpg` and `T10_metadata.json` from the robot into the local raw-data directory. The image is 640x360 RGB, 71,920 bytes, and its SHA-256 `958de8f87d614a409e639473c84523b9f1939a9ebe26c5c4fb962a39c83e4703` matches the sidecar exactly. Visual inspection found the object fully visible and no target cubes. Maher reviewed the displayed image and approved it for training. This completes the planned training-scene coverage.

No captures were annotated, imported into the YOLO dataset, or used for training during this transfer and review step. Next learning checkpoint: Maher reviews the transferred captures, beginning with T10, before annotation.

### Training capture retention approved

After reviewing the training overview and T10 individually, Maher confirmed that T01-T10 should all be retained for training. This approval concerns image suitability and intended split. It is not yet approval of bounding boxes or class annotations, because those have not been prepared or displayed. The raw images remain unchanged.

## Provisional annotations and review gallery prepared

After Maher authorized the annotation step, Codex visually inspected every positive original at 640x360 resolution. A color-region analysis was used only to suggest likely cube locations; it also reacted to colored background objects, so its output was not accepted automatically. Codex selected and adjusted each proposed box to cover the complete visible cube and exclude its cast shadow. The resulting provisional record is `evaluation/robot_dataset_2026-09-12/proposed_annotations.json`: 18 images, 30 boxes, class names, pixel `xyxy` coordinates, source-image SHA-256 values, and the preassigned training or validation role. T09, T10, V07, and V08 intentionally contain zero target boxes.

Added `scripts/build_annotation_review.py`, which reads the provisional JSON and creates a read-only HTML gallery with the original JPEGs embedded below numbered SVG overlays. The generator does not alter the JPGs, create YOLO labels, or mark proposals as approved. Generated `evaluation/results/saturday-annotation-review/index.html` with 18 image panels and 30 rectangles, then checked the rendered overlays in separate training and validation contact sheets. Python bytecode compilation and structural HTML counts passed.

The new gallery is the next human-review checkpoint. Maher must confirm or correct the boxes and class names before a confirmed review record or normalized YOLO text labels are generated. No new images were imported into the training dataset, and no training or evaluation ran.

### Annotation review confirmed

Maher opened the generated HTML gallery, reviewed the displayed annotations, and confirmed that the boxes and class names look correct. Preserved the provisional proposal and created `evaluation/robot_dataset_2026-09-12/human_review.json` as the separate confirmed record. Every image entry is marked confirmed, with Maher as reviewer and the review date recorded. The confirmed record still uses pixel-corner boxes; normalized YOLO label files have not yet been generated.

Next learning checkpoint: explain the dataset exporter and let Maher inspect the intended training/validation counts and class mapping before executing the export. No training or evaluation ran.

## Existing dataset exporter generalized and checked

Maher asked whether an existing script could perform the export instead of adding another overlapping tool. Inspected the scripts directory and selected `scripts/prepare_reviewed_robot_dataset.py`: it already contained the reviewed-image checks, pixel-to-YOLO conversion, round-trip coordinate verification, image/label copying, YAML generation, and manifest generation. `normalize_dataset.py` and `build_hardneg_dataset.py` serve different historical formats and were not suitable replacements.

With Maher's approval, generalized the existing exporter. Added command-line inputs for review, legacy source root, output, and manifest, plus `--check-only`. New review records may specify a source file and an explicit `training` or `validation` split per image; historical review records without those fields retain the fixed 9 September defaults. The check-only path performs review-status, source-hash, duplicate, image-dimension, class, box-bound, coordinate round-trip, split-overlap, and class-coverage checks, then exits before creating any directory or file.

Verification used Python bytecode compilation and two check-only runs. The new review passed with ten training images, twelve boxes split evenly across the three colors, two training negatives, eight validation images, eighteen boxes split evenly across the three colors, and two validation negatives. The historical default review also passed with its original 22/4 image allocation. `git diff --check` passed. No dataset export, training, or evaluation ran.

The new review covers only the newly captured images. The intended final training set also retains the previously selected 13 training pairs, so exporting the new review alone would produce ten rather than 23 training images. Before the real export, the existing selected-13 pairs must be incorporated explicitly and verified rather than assumed from the old 26-image review.

### Existing selected-13 training base incorporated in preflight

Maher approved extending the same exporter to retain the existing selected-13 dataset. Added paired `--base-training-dataset` and `--base-manifest` inputs. When supplied, the exporter enumerates the base training JPG and TXT files, requires exact stem pairing, looks up each image in the historical manifest, verifies its source hash, reconstructs the YOLO label from the approved pixel boxes, and requires exact equality with the existing TXT file. Only then are those pairs staged in memory alongside the new reviewed images. The base contributes training examples only; the fresh V01-V08 split remains validation.

The combined check-only run passed with 23 training images, 39 cube boxes distributed as 13 per class, and three background-only training images. Validation contains eight images, 18 cube boxes distributed as six per class, and two background-only images. The historical no-argument check-only path also passed with its original 22/4 allocation. Python compilation, `git diff --check`, and the assertion that check-only created neither the new output directory nor its manifest all passed.

No dataset export has run. The next guided step is for Maher to execute the displayed non-check-only command, after reviewing that removing `--check-only` is the operation that writes the new dataset.

## Combined 23/8 YOLO dataset exported and verified

Maher manually ran the generalized exporter without `--check-only`, using the confirmed 12 September review, the selected-13 base dataset and its historical manifest, a new 12 September output directory, and a new split manifest. The command completed in export mode and reported the counts predicted by the preflight.

Codex independently inspected the written files. `evaluation/results/robot_training_2026-09-12` contains 23 matching JPG/TXT pairs in training and eight matching pairs in validation. Training has 13 labels for each cube color and three empty negative label files; validation has six labels for each color and two empty negative label files. All 31 exported image hashes match the new manifest. Every nonempty label line has one valid class ID plus four normalized values, and `data.yaml` maps 0 to blue, 1 to green, and 2 to red while pointing to the new train and validation directories. `git diff --check` passed.

This verifies dataset construction, not model quality. No fine-tuning or evaluation has run on this new 23/8 export. Next learning checkpoint: connect the training script to the new `data.yaml`, inspect the exact settings and starting checkpoint, then let Maher launch training manually.

### Training script connected to the new dataset

Maher authorized Codex to apply the previously displayed path change in `scripts/run_robot_training_experiment.py`. The script now imports only the shared project root, general pretrained checkpoint, and hash helper from the smoke script. Its evidence directory points to the confirmed 12 September review and manifest, its data path points to the new 23/8 `data.yaml`, and its run parent is a new 12 September directory. No optimizer, augmentation, epoch, checkpoint, or training-call setting changed.

Verification compiled the file and imported it without calling `main()`, then asserted that all three configured locations resolve as intended and that the review, manifest, and YAML inputs exist. The first import harness omitted the scripts directory from Python's module search path and therefore could not resolve the sibling smoke module; this was a harness setup error. Repeating the import with the same search path Python uses for direct script execution passed. `git diff --check` passed. No CUDA operation, model loading, training, or output-directory creation occurred.

Before launching, the stale summary limitation describing four validation images must be updated to the current eight-image validation set, and the existing training settings should be reviewed against the new dataset rather than assumed correct.

### Training summary limitation corrected

With Maher's approval, updated the `limitation` field written to each run's `summary.json`. It now describes the actual eight-image validation set captured in the same room and session, and states that live robot testing remains separate. This is metadata for interpreting the result; it does not change model training, validation calculations, or checkpoint selection.

Python bytecode compilation and `git diff --check` passed. No model was loaded and no training or evaluation ran during this edit. The next learning checkpoint is a complete non-training preflight of the script before Maher launches fine-tuning manually.

### Final non-training preflight completed

At Maher's request, ran a read-only preflight that separated dataset integrity checks from the actual training call. The general system Python did not contain PyTorch, so the project environment `.venv-m2` was confirmed as the required interpreter. An initial attempt to extract the settings dictionary with `ast.literal_eval` stopped at dynamic expressions such as `str(DATA)`; this was a limitation of that inspection helper, not a defect in the training script. The file verifier was rerun without trying to evaluate those dynamic expressions.

The successful verifier checked the starting `yolov5su.pt` file against its expected SHA-256, matched the confirmed review hash to the split manifest, checked every exported JPG hash, required exact JPG/TXT stem pairs, compared label-row counts with approved object counts, and required valid class IDs and normalized coordinates. It found 23 training image/label pairs and eight validation pairs with no filename overlap. Training contains 13 boxes per color; validation contains six boxes per color. The environment reports PyTorch 2.6.0 with CUDA 12.4 support and Ultralytics 8.4.75.

CUDA visibility was false inside the restricted command sandbox, so a separately authorized host check was used. That check confirmed CUDA access and identified the NVIDIA GeForce RTX 4070 Ti. This distinction matters because a sandbox result does not describe the host GPU available to the real training command.

Reviewed the settings directly from `scripts/run_robot_training_experiment.py`: 60 maximum epochs, 640-pixel input, batch and nominal batch size four, GPU device zero, deterministic seed 42, AdamW with initial learning rate 0.001 and final multiplier 0.1, no warm-up, patience 15, automatic mixed precision disabled, and mild brightness, scale, translation, and horizontal-flip augmentation. Mosaic and mixup are disabled. Training will start from the verified general pretrained YOLOv5u-small checkpoint, reserve a new timestamped run directory, save settings and plots, validate on the eight held-out images, and write `best.pt`, `last.pt`, `results.csv`, and `summary.json`.

No checkpoint was loaded and no training or validation inference ran during this preflight. The paths, files, labels, split, environment, and GPU access are ready for Maher to start the real training command manually.

#### Preflight reference tables

These tables preserve the exact pre-training reference shown to Maher before the run.

| Role | File or directory | Verified result |
|---|---|---|
| Training script | `scripts/run_robot_training_experiment.py` | Python syntax valid |
| Starting model | `models/pretrained/yolov5su.pt` | Expected SHA-256; general pretrained YOLOv5u-small |
| Dataset configuration | `evaluation/results/robot_training_2026-09-12/data.yaml` | Paths and class mapping valid |
| Training data | `evaluation/results/robot_training_2026-09-12/images/train` and `labels/train` | 23 exact image/label pairs |
| Validation data | `evaluation/results/robot_training_2026-09-12/images/val` and `labels/val` | Eight separate image/label pairs |
| Confirmed annotations | `evaluation/robot_dataset_2026-09-12/human_review.json` | Hash matches the manifest |
| Split manifest | `evaluation/robot_dataset_2026-09-12/development_split.json` | 31 images recorded and verified |
| Run parent | `runs/robot_2026-09-12` | A timestamped child is created only when training starts |
| Python environment | `.venv-m2` | PyTorch 2.6.0+cu124 and Ultralytics 8.4.75 |
| GPU | NVIDIA GeForce RTX 4070 Ti | CUDA accessible in the host environment |

| Split | Images | Blue boxes | Green boxes | Red boxes |
|---|---:|---:|---:|---:|
| Training | 23 | 13 | 13 | 13 |
| Validation | 8 | 6 | 6 | 6 |

The training and validation image filename sets are disjoint.

| Setting | Value | Purpose in this experiment |
|---|---:|---|
| `epochs` | 60 | Maximum complete passes through the training set |
| `imgsz` | 640 | Model input size |
| `batch` / `nbs` | 4 / 4 | Four images per batch and one update per batch |
| `device` | 0 | First NVIDIA GPU |
| `workers` | 0 | Load data in the training process, avoiding extra worker processes |
| `optimizer` | `AdamW` | Weight-update algorithm |
| `lr0` / `lrf` | 0.001 / 0.1 | Initial learning rate and final learning-rate multiplier |
| `warmup_epochs` | 0.0 | No warm-up phase |
| `patience` | 15 | Stop after 15 epochs without a better validation fitness score |
| `seed` | 42 | Reproducible pseudo-random choices |
| `deterministic` | `True` | Request deterministic training behavior where supported |
| `amp` | `False` | Disable automatic mixed precision |
| `hsv_h` / `hsv_s` / `hsv_v` | 0.0 / 0.0 / 0.1 | No hue or saturation shift; mild brightness variation |
| `scale` / `translate` | 0.1 / 0.05 | Mild random size and position changes |
| `fliplr` / `flipud` | 0.5 / 0.0 | Possible horizontal reflection; no vertical reflection |
| `mosaic` / `mixup` | 0.0 / 0.0 | Disable both image-combination augmentations |
| `close_mosaic` | 0 | No mosaic-closing phase because mosaic is disabled |
| `cache` | `False` | Read images without preloading the complete dataset cache |
| `pretrained` | `True` | Retain transfer-learning behavior with the loaded general checkpoint |
| `val` / `plots` / `save` | `True` | Validate and retain plots and checkpoints |

The run will write `requested_settings.json` before model loading, followed by Ultralytics training artifacts such as `results.csv`, plots, `weights/best.pt`, and `weights/last.pt`. After the scripted post-training checks pass, it will also write `summary.json`.

## Fine-tuning run completed and results preserved

Maher manually executed:

```bash
.venv-m2/bin/python scripts/run_robot_training_experiment.py
```

The run completed in `runs/robot_2026-09-12/experiment_60ep_20260912_221210+0200`. Maher's pasted terminal output was copied unchanged into that run as `terminal_output.txt`, alongside the automatically generated `requested_settings.json`, `args.yaml`, `results.csv`, plots, validation previews, confusion matrices, `weights/best.pt`, `weights/last.pt`, and `summary.json`.

The log confirms the intended environment and inputs: Ultralytics 8.4.75, PyTorch 2.6.0+cu124, the NVIDIA GeForce RTX 4070 Ti, the general pretrained `yolov5su.pt` checkpoint, 23 training images with three backgrounds, and eight validation images with two backgrounds. All 60 configured epochs completed in about 0.012 hours. The best validation result occurred at epoch 45. The patience counter reached 15 at epoch 60, which coincided with the configured maximum; therefore the run did not omit a planned epoch.

The script then reloaded and validated `weights/best.pt`. The final best-checkpoint results were:

| Class | Validation images | Instances | Precision | Recall | mAP50 | mAP50-95 |
|---|---:|---:|---:|---:|---:|---:|
| All | 8 | 18 | 0.982 | 0.964 | 0.995 | 0.707 |
| Blue cube | 6 | 6 | 0.957 | 1.000 | 0.995 | 0.788 |
| Green cube | 6 | 6 | 0.989 | 1.000 | 0.995 | 0.609 |
| Red cube | 6 | 6 | 1.000 | 0.892 | 0.995 | 0.723 |

The overall values in `summary.json` match the final best-checkpoint validation: precision 0.9818655, recall 0.9639927, mAP50 0.995, and mAP50-95 0.7066556. The saved best checkpoint is 18,498,235 bytes, and its SHA-256 matches the checkpoint hash recorded in the summary. `last.pt` is preserved separately. The requested settings agree with Ultralytics' saved arguments; `device` is represented as integer `0` in the request and string `"0"` in `args.yaml`, which is a serialization difference rather than a changed GPU choice.

The first-epoch overall precision, recall, mAP50, and mAP50-95 were approximately 0.00151, 0.05556, 0.00025, and 0.00011. By epoch 60, the corresponding current-epoch measurements were approximately 0.96879, 0.99764, 0.995, and 0.65379. The selected epoch-45 checkpoint has the stronger mAP50-95 of approximately 0.70666, which is why `best.pt` rather than `last.pt` is the candidate for the next stage.

These are development-validation measurements from eight new images captured in the same room and session. They provide evidence that the model learned and generalized to those held-out arrangements, but they are not yet evidence of reliable live JetRover performance. The next major stage is to inspect the saved predictions and confusion matrix together before exporting or deploying the checkpoint.
